{{ form_start(form) }}
<div class="form-group">
    {{ form_errors(form) }}

    {{ form_row(form.email)}}
    {{ form_row(form.roles)}}

    {{ form_row(form.nom)}}
    {{ form_row(form.prenom)}}
    <button type ="button" class="btn btn-warning">{{ button_label|default('Save') }}</button>
</div>
{{ form_end(form) }}
