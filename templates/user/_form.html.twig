{{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.email)}}
    {{ form_row(form.roles)}}
  
    {{ form_row(form.nom)}}
    {{ form_row(form.prenom)}}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
